# Changelog

## [Unreleased]
### Added
- 剰余演算子 `%` を追加
- 条件論理演算子 `&&` / `||` の短絡評価を実装
- 後置インクリメント/デクリメント（`a++` / `a--`）を追加
- 文字列連結演算子（`"a" + "b"`、`s += "..."`）を追加（VM命令 `Sadd` を新設）

### Changed
- 式コンパイラのレジスタ受け渡し規約を再設計（式中の関数呼び出しが評価途中の値を破壊するバグを根治）
- 代入・複合代入・ローカル変数初期化・return・引数渡しに型検査を追加（暗黙変換は 整数→実数 の昇格と 文字列⇔オブジェクト のみ許可）
- グローバル変数の初期化子が実行時に反映されるように修正

### Compatibility
- `Sadd` 命令を使用するバイナリは、旧バージョンのVMでは実行できません（VMとコンパイラは同時に更新してください）
